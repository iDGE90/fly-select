import { SelectGroupBodyDirective } from './select-group-body.directive';

describe('SelectGroupBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectGroupBodyDirective();
    expect(directive).toBeTruthy();
  });
});

import { SelectItemBodyDirective } from './select-item-body.directive';

describe('SelectItemBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectItemBodyDirective();
    expect(directive).toBeTruthy();
  });
});
